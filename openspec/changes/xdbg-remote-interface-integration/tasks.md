## 1. 远程接口适配层

- [x] 1.1 在核心层新增 x64dbg 远程适配模块（连接、握手、命令发送、响应解析）
- [x] 1.2 定义远程配置结构（endpoint、token、timeout、retry）并接入应用启动配置
- [x] 1.3 建立错误分类映射（连接失败、鉴权失败、超时、协议错误）

## 2. 会话管理与动作映射

- [x] 2.1 在 `src/core/session.rs` 增加远程会话状态机（disconnected/connecting/connected/degraded）
- [x] 2.2 在 `src/core/engine.rs` 增加远程动作分发入口（continue/step over/step in/pause）
- [x] 2.3 映射读寄存器与读内存动作到远程接口并规范化返回格式
- [x] 2.4 增加断线重连策略（指数退避或固定间隔）与状态恢复逻辑

## 3. UI 最小化接入

- [x] 3.1 在 `src/ui/control_panel.rs` 增加 endpoint/token 输入与 Connect/Disconnect 按钮
- [x] 3.2 在 `src/ui/status_bar.rs` 增加远程连接状态徽章与错误摘要
- [x] 3.3 保持现有布局不重构，仅接入必要控件和动作透传
- [x] 3.4 连接失败时提供可读错误提示，不影响本地调试回退路径
- [x] 3.5 调整按钮子元素颜色策略：按钮内文字/图标前景色与按钮背景色分离，避免同色

## 4. 测试任务

- [x] 4.1 为命令映射与错误分类补充单元测试
- [x] 4.2 为会话状态迁移补充单元测试（含异常迁移和恢复）
- [x] 4.3 增加远程连接集成测试（成功、超时、鉴权失败、断开重连）
- [x] 4.4 增加关键动作链路测试（continue/step/pause/read-register/read-memory）

## 5. 文档任务

- [x] 5.1 新增 `docs/xdbg-remote-integration.md`，说明连接参数和启动方式
- [x] 5.2 在文档中提供“核心动作触发方式”对照表（UI 操作 -> 远程命令）
- [x] 5.3 增加故障排查章节（连接失败、超时、返回格式异常）

## 6. 验证清单

- [x] 6.1 本地 mock 远程端验证连接与动作执行可达成
- [ ] 6.2 真实 x64dbg 远程端验证最小闭环（连接 -> 动作 -> 状态反馈）
- [x] 6.3 确认未引入自研复杂 UI 子系统，变更保持最小化
- [x] 6.4 验证按钮文字/图标在 normal/hover/disabled 状态下均不与按钮底色同色
